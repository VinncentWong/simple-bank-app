spring:
  application:
    name: user-service
  cloud:
    config:
      uri: http://localhost:8000

server:
  port: 8002

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org:
      hibernate:
        stat: debug
        type: trace
        orm:
          jdbc:
            bind: trace
